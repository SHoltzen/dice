fun func1(a: bool, b: (bool, bool)) {
  let x = ((fst b) && flip 0.6, (snd b) && flip 0.7) in
  a || (fst x) || (snd x)
}

fun func2(a: (bool, bool), b: bool) {
  b || func1(b, a)
}

let msg = flip 0.8 in
let t1 = func2((msg && flip 0.9, true), func1(msg, (flip 0.8, false))) in
func1(t1, (flip 0.9, flip 0.9)) || flip 0.6

